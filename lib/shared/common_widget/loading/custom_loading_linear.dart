import 'package:flutter/material.dart';

class CustomLoadingLinear extends StatelessWidget {
  const CustomLoadingLinear({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: (LinearProgressIndicator()));
  }
}
